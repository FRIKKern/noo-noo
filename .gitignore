# Go
*.exe
*.test
*.prof
*.out
/bin/
/dist/

# Wails (added in v0.3) — ignore compiled app bundles produced by `wails3 build`
# but keep the source-tree build/ directory under cmd/noo-noo-app/ which holds
# the appicon and other Wails build inputs. Also un-ignore the source-tree
# build/release/ (Phase 0.4 release scripts) and build/brew/ (formula
# templates) which are committed source, not build outputs.
build/
!cmd/noo-noo-app/build/
!build/release/
!build/brew/
frontend/dist/
frontend/node_modules/
frontend/.svelte-kit/

# Node
node_modules/
npm-debug.log*

# IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Local dev artifacts
*.db
*.db-journal
.env
.env.local
.verify-blocked
verification-results/
